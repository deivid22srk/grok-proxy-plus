<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The proxy binds a local socket and reaches xAI over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service that keeps the Go runtime alive while backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- specialUse type (Android 14+): a local HTTP proxy doesn't fit the typed
         categories; the subtype is declared inside <service> below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Runtime permission (Android 13+) so the FGS notification is visible. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrokProxyPlus"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.GrokProxyPlus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ProxyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Required by Android 14+ for the specialUse FGS type. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local HTTP proxy server exposing OpenAI/Anthropic-compatible API endpoints on the device" />
        </service>
    </application>

</manifest>
