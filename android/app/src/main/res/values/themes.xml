<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Base theme delegates all chrome to Jetpack Compose (Material 3 with
      dynamic color on Android 12+). This is an empty Material3 base so the
      activity has no legacy ActionBar and the Compose content fills the window.
    -->
    <style name="Theme.GrokProxyPlus" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
